# RadioGardenAPI Plugin

> **Важно:** Данный плагин создан при помощи языковой модели (LLM) в целях тестирования и прототипирования. Не рекомендуется использовать в production без дополнительного тестирования и аудита кода.

Плагин для Unreal Engine 5.6, предоставляющий API для работы с [Radio Garden](https://radio.garden/) - сервисом для прослушивания радиостанций со всего мира.

**Плагин предоставляет доступ к API Radio Garden**, позволяя искать радиостанции по геолокации, получать прямые ссылки на потоки и работать с метаданными станций.

## Возможности

### Основная функциональность
- **Поиск ближайших станций** - поиск радиостанций по координатам или автоматической геолокации
- **Получение мест** - список всех городов/локаций с радиостанциями
- **Каналы места** - получение всех станций в конкретной локации
- **Информация о станции** - метаданные радиостанции (название, место, страна)
- **Прямые ссылки на потоки** - URL для воспроизведения в формате MP3
- **Поиск станций** - поиск по названию станции, места или страны
- **Геолокация** - автоматическое определение location клиента

### Дополнительные функции
- **Сортировка по расстоянию** - автоматический расчет расстояния от точки до станции
- **Агрегация результатов** - сбор нужного количества станций с нескольких близлежащих мест
- **Синхронный и асинхронный режим** - все функции доступны в обоих режимах
- **Blueprint API** - полное использование без C++ кода

## Требования

- Unreal Engine 5.6 или выше
- Активное подключение к интернету

## Установка

1. Скопируйте папку `RadioGardenAPI` в директорию `Plugins/` вашего проекта
2. Перезапустите Unreal Editor
3. Активируйте плагин через меню **Edit → Plugins → Radio Garden API**
4. Перекомпилируйте проект при запросе

## Использование

### Blueprint API

#### Получение ближайших станций по координатам

Функция: **Get Nearby Channels**
- Параметры: `Latitude` (double), `Longitude` (double), `Channels Count` (int32)
- Возвращает: `FRadioGardenNearbyChannelsResponse`
  - `bSuccessful` (bool) - успех операции
  - `Channels` (Array) - массив станций с полями:
    - `Url` (string) - прямая ссылка на поток
    - `Title` (string) - название станции
    - `Distance` (double) - расстояние в километрах
  - `Error Message` (string) - текст ошибки

#### Получение ближайших станций по геолокации

Функция: **Get Nearby Channels By Geolocation**
- Параметры: `Channels Count` (int32)
- Возвращает: `FRadioGardenNearbyChannelsResponse`
- Автоматически определяет местоположение клиента и возвращает ближайшие станции

#### Получение всех мест

Функция: **Get Places**
- Параметры: отсутствуют (синхронно) или `On Completed` делегат (асинхронно)
- Возвращает: `FRadioGardenPlacesResponse`
  - `Places` (Array of `FRadioGardenPlace`)
    - `Id` (string) - уникальный ID места
    - `Title` (string) - название места
    - `Country` (string) - страна
    - `Geo` (Coords) - координаты (широта, долгота)
    - `Size` (int32) - количество станций
    - `Url` (string) - URL места

#### Получение станций в месте

Функция: **Get Place Channels**
- Параметры: `Place Id` (string)
- Возвращает: `FRadioGardenChannelsResponse`
  - `Channels` (Array of `FRadioGardenChannel`)
    - `Id` (string) - ID станции
    - `Title` (string) - название
    - `Url` (string) - URL станции
    - `Place Id`, `Place Title` - информация о месте
    - `Country Id`, `Country Title` - информация о стране

#### Получение информации о станции

Функция: **Get Channel**
- Параметры: `Channel Id` (string)
- Возвращает: `FRadioGardenChannelResponse`
  - `Channel` (`FRadioGardenChannel`) - полная информация о станции
    - Все поля из `Get Place Channels` плюс:
    - `Website` (string) - веб-сайт станции
    - `bSecure` (bool) - безопасное соединение
    - `Stream Url` (string) - прямая ссылка на поток

#### Получение прямой ссылки на поток

Функция: **Get Channel Stream Url**
- Параметры: `Channel Id` (string)
- Возвращает (через делегат):
  - `bSuccess` (bool) - успех операции
  - `Stream Url` (string) - прямая ссылка на MP3 поток
- Формат URL: `https://radio.garden/api/ara/content/listen/{id}/channel.mp3`

#### Поиск станций

Функция: **Search**
- Параметры: `Query` (string) - поисковый запрос
- Возвращает: `FRadioGardenSearchResponse`
  - `Results` (Array of `FRadioGardenSearchResult`)
    - `Id` (string) - ID результата
    - `Type` (string) - тип: "channel", "place", "country"
    - `Title` (string) - название
    - `Subtitle` (string) - подзаголовок
    - `Url` (string) - URL
    - `Score` (float) - релевантность

#### Геолокация

Функция: **Get Geolocation**
- Параметры: отсутствуют
- Возвращает: `FRadioGardenGeolocationResponse`
  - `Geolocation` (`FRadioGardenGeolocation`)
    - `Ip` (string) - IP адрес
    - `Country Code`, `Country Name` - страна
    - `Region Code`, `Region Name` - регион
    - `City` (string) - город
    - `Latitude`, `Longitude` (double) - координаты
    - `Time Zone` (string) - часовой пояс
    - `Zip Code` (string) - почтовый индекс

#### Вспомогательные функции

- **Is Response Successful** - проверка успешности ответа (bool)
- **Get Error Message** - получение текста ошибки (string)
- **Get API Base Url** - получение базового URL API (string)
- **Coords To String** - конвертация координат в строку (string)

## Типы данных

### FRadioGardenChannelWithDistance
- `Url` (string) - прямая ссылка на поток MP3
- `Title` (string) - название станции
- `Distance` (double) - расстояние в километрах

### FRadioGardenPlace
- `Id` (string) - уникальный ID места
- `Title` (string) - название места (например, "Yerevan")
- `Country` (string) - страна
- `Url` (string) - URL
- `Geo` (Coords) - координаты (широта, долгота)
- `Size` (int32) - количество станций в месте
- `bBoost` (bool) - популярное место

### FRadioGardenChannel
- `Id` (string) - ID станции
- `Title` (string) - название
- `Url` (string) - URL станции
- `Website` (string) - веб-сайт станции
- `bSecure` (bool) - безопасное соединение
- `PlaceId`, `PlaceTitle` (string) - информация о месте
- `CountryId`, `CountryTitle` (string) - информация о стране
- `StreamUrl` (string) - прямая ссылка на поток

### FRadioGardenGeolocation
- `Ip` (string) - IP адрес
- `CountryCode`, `CountryName` (string) - страна
- `RegionCode`, `RegionName` (string) - регион
- `City` (string) - город
- `ZipCode` (string) - почтовый индекс
- `TimeZone` (string) - часовой пояс
- `Latitude`, `Longitude` (double) - координаты
- `MetroCode` (int32) - metro code

## Делегаты

- **FOnRadioGardenPlacesReceived** - используется в `GetPlaces`, `GetPlaceDetails`
- **FOnRadioGardenChannelsReceived** - используется в `GetPlaceChannels`
- **FOnRadioGardenChannelReceived** - используется в `GetChannel`
- **FOnRadioGardenStreamUrlReceived** - используется в `GetChannelStreamUrl`
- **FOnRadioGardenSearchCompleted** - используется в `Search`
- **FOnRadioGardenGeolocationReceived** - используется в `GetGeolocation`
- **FOnRadioGardenNearbyChannelsReceived** - используется в `GetNearbyChannels`, `GetNearbyChannelsByGeolocation`

## API Endpoints

Плагин использует следующие endpoint'ы Radio Garden API:

- `GET /ara/content/places` - список всех мест
- `GET /ara/content/page/:placeId/channels` - станции в месте
- `GET /ara/content/channel/:channelId` - информация о станции
- `GET /ara/content/listen/:channelId/channel.mp3` - прямой поток (редирект)
- `GET /search?q=query` - поиск
- `GET /geo` - геолокация клиента

Базовый URL: `https://radio.garden/api/ara`

## Алгоритм работы GetNearbyChannels

1. **Получение всех мест** - запрос всех мест (12,000+ локаций)
2. **Расчет расстояний** - вычисление расстояния от точки до каждого места (формула Хаверсина)
3. **Сортировка мест** - по возрастанию расстояния
4. **Сбор каналов** - итерация по местам от ближайшего к дальнему:
   - Запрос каналов места
   - Добавление каналов в общий список
   - Прерывание когда собрано нужное количество
5. **Финальная сортировка** - сортировка всех каналов по расстоянию
6. **Ограничение** - возврат только запрошенного количества

**Особенности:**
- Если в ближайшем месте недостаточно станций, берутся станции из следующих мест
- Каналы сортируются по расстоянию до места (не до конкретной станции)
- Каждый канал получает URL потока в формате: `https://radio.garden/api/ara/content/listen/{id}/channel.mp3`

## Технические детали

### Производительность
- ⚡ Среднее время получения ближайших станций: 3-8 секунд (зависит от количества мест)
- ⏱️ Таймаут HTTP запросов: 30 секунд
- 🌍 База мест: ~12,000 локаций по всему миру
- 📊 Формула расстояния: Хаверсин (точность ~0.5%)

### Версионность движка
- **Unreal Engine 5.6+**

### Логирование
- Категория: `LogRadioGardenAPI`
- Уровни: Log, Warning, Error, Verbose

## Известные ограничения

1. Требуется активное подключение к интернету
2. Количество запросов к API не должно быть слишком частым (recommended rate limiting)
3. Полученные URL потоков могут иметь ограниченный срок действия
4. Доступность потоков зависит от статиса радиостанции
5. Геолокация основана на IP-адресе клиента (может быть неточной при использовании VPN)
